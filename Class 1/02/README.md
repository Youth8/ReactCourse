#### Demo for building page with React npm package

1. Download and install [Nodejs](www.nodejs.org).
2. Run command in terminal to install dependencies
```shell
npm i
```
3. Run command in terminal to run up DEMO.
```shell
npm run start
```